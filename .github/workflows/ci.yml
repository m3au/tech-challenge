name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  # eslint-disable-next-line yml/no-empty-mapping-value -- GitHub Actions allows empty trigger events
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  preflight:
    name: Pre-flight Checks
    uses: ./.github/workflows/preflight.yml

  test:
    name: E2E Tests
    needs: [preflight]
    if: github.actor != 'dependabot[bot]'
    uses: ./.github/workflows/test.yml
    secrets: inherit

  lighthouse:
    name: Lighthouse Audit
    needs: [preflight]
    if: github.actor != 'dependabot[bot]'
    uses: ./.github/workflows/lighthouse.yml
    secrets: inherit

  axe:
    name: Axe Audit
    needs: [preflight]
    if: github.actor != 'dependabot[bot]'
    uses: ./.github/workflows/axe.yml
    secrets: inherit

  publish:
    name: Publish Reports
    needs: [preflight, test, lighthouse, axe]
    if: always() && github.ref == 'refs/heads/main' && github.actor != 'dependabot[bot]'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.1

      - name: Install dependencies
        run: bun install

      - name: Download blob reports
        uses: actions/download-artifact@v4
        continue-on-error: true
        with:
          pattern: blob-report-shard-*
          merge-multiple: true
          path: test-output/blob-reports/

      - name: Merge test reports
        continue-on-error: true
        run: |
          bunx playwright merge-reports \
            --reporter=html \
            test-output/blob-reports
          if [ -d "playwright-report" ]; then
            mkdir -p test-output/playwright-report
            mv playwright-report/* test-output/playwright-report/ || true
            rmdir playwright-report || true
          fi

      - name: Generate challenge summary
        continue-on-error: true
        run: bun scripts/generate-challenge-summary.ts

      - name: Generate bugs reports
        continue-on-error: true
        run: bun scripts/generate-bugs-report.ts

      - name: Download challenge summary
        uses: actions/download-artifact@v4
        continue-on-error: true
        with:
          name: challenges-summary
          path: test-output/challenges/
          if-no-files-found: ignore

      - name: Download Lighthouse reports
        uses: actions/download-artifact@v4
        continue-on-error: true
        with:
          name: lighthouse-reports
          path: test-output/lighthouse/

      - name: Download Axe reports
        uses: actions/download-artifact@v4
        continue-on-error: true
        with:
          name: axe-reports
          path: test-output/axe/

      - name: Download unit test reports
        uses: actions/download-artifact@v4
        continue-on-error: true
        with:
          name: unit-tests-report
          path: test-output/unit-tests/

      - name: Download unit test coverage (fallback)
        uses: actions/download-artifact@v4
        continue-on-error: true
        with:
          name: unit-tests-coverage
          path: test-output/unit-tests-coverage/

      - name: Create placeholder pages for missing reports
        run: |
          mkdir -p test-output/lighthouse test-output/axe test-output/playwright-report test-output/unit-tests test-output/challenges
          if [ ! -f "test-output/lighthouse/index.html" ]; then
            cp .github/templates/lighthouse-report.html test-output/lighthouse/index.html
          fi
          if [ ! -f "test-output/axe/index.html" ]; then
            cp .github/templates/axe-report.html test-output/axe/index.html
          fi
          if [ ! -f "test-output/playwright-report/index.html" ]; then
            echo '<!doctype html><html><head><title>Playwright Report Unavailable</title></head><body><h1>Playwright Report Unavailable</h1><p>No test reports were generated. Check CI logs for details.</p></body></html>' > test-output/playwright-report/index.html
          fi
          if [ ! -f "test-output/challenges/index.html" ]; then
            bun scripts/generate-challenge-summary.ts
          fi
          if [ ! -f "test-output/unit-tests/index.html" ]; then
            cp .github/templates/unit-tests-report.html test-output/unit-tests/index.html
          fi
          if [ -f "test-output/unit-tests/index.html" ] && [ ! -f "test-output/unit-tests/lcov.info" ]; then
            if [ -f "test-output/unit-tests-coverage/lcov.info" ]; then
              cp test-output/unit-tests-coverage/lcov.info test-output/unit-tests/lcov.info
              echo "✓ Copied lcov.info from unit-tests-coverage artifact"
            fi
          fi

      - name: Copy reports index template
        run: |
          mkdir -p test-output
          cp .github/templates/reports-index.html test-output/index.html

      - name: Setup Pages
        uses: actions/configure-pages@v4
        with:
          enablement: true

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: test-output

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

      - name: Add report dashboard summary
        if: always()
        run: |
          cat >> $GITHUB_STEP_SUMMARY << 'EOF'
          ## 📊 Test Reports Dashboard

          **Interactive HTML Reports:** [https://m3au.github.io/playwright-bdd-cursor-template/](https://m3au.github.io/playwright-bdd-cursor-template/)

          ### Available Reports

          - 🎭 [E2E Test Results](https://m3au.github.io/playwright-bdd-cursor-template/playwright-report/)
          - 🗼 [Lighthouse Performance](https://m3au.github.io/playwright-bdd-cursor-template/lighthouse/)
          - ♿ [Axe Accessibility](https://m3au.github.io/playwright-bdd-cursor-template/axe/)
          - 🧪 [Unit Tests Coverage](https://m3au.github.io/playwright-bdd-cursor-template/unit-tests/)

          ---

          *Reports updated on every push to main*
          EOF

          echo "::notice title=Test Reports Dashboard::View interactive reports at https://m3au.github.io/playwright-bdd-cursor-template/"
